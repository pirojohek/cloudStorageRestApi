spring:
  datasource:
    url: jdbc:postgresql://localhost:54123/users_storage
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true


  data:
    redis:
      host: localhost
      port: 61379
      password: ""
  session:
    store-type: redis
    timeout: 30m


minio:
  url: http://localhost:9100
  access-key: myuser
  secret-key: mypassword
  bucket: user-files
